export const commonWordDefinitions = {
  'a': { phonetic: '/ə/', definition: 'art. 一个' },
  'an': { phonetic: '/ən/', definition: 'art. 一个' },
  'the': { phonetic: '/ðə/', definition: 'art. 这个；那个' },
  'is': { phonetic: '/ɪz/', definition: 'v. 是' },
  'are': { phonetic: '/ɑːr/', definition: 'v. 是' },
  'was': { phonetic: '/wʌz/', definition: 'v. 是（过去式）' },
  'were': { phonetic: '/wɜːr/', definition: 'v. 是（过去式）' },
  'be': { phonetic: '/biː/', definition: 'v. 是；成为' },
  'been': { phonetic: '/bɪn/', definition: 'v. 是（过去分词）' },
  'being': { phonetic: '/ˈbiːɪŋ/', definition: 'n. 存在；生物' },
  'have': { phonetic: '/hæv/', definition: 'v. 有' },
  'has': { phonetic: '/hæz/', definition: 'v. 有' },
  'had': { phonetic: '/hæd/', definition: 'v. 有（过去式）' },
  'do': { phonetic: '/duː/', definition: 'v. 做' },
  'does': { phonetic: '/dʌz/', definition: 'v. 做' },
  'did': { phonetic: '/dɪd/', definition: 'v. 做（过去式）' },
  'will': { phonetic: '/wɪl/', definition: 'v. 将；愿意' },
  'would': { phonetic: '/wʊd/', definition: 'v. 将；愿意' },
  'could': { phonetic: '/kʊd/', definition: 'v. 能；可以' },
  'should': { phonetic: '/ʃʊd/', definition: 'v. 应该' },
  'may': { phonetic: '/meɪ/', definition: 'v. 可能；可以' },
  'might': { phonetic: '/maɪt/', definition: 'v. 可能' },
  'shall': { phonetic: '/ʃæl/', definition: 'v. 将；应该' },
  'can': { phonetic: '/kæn/', definition: 'v. 能；可以' },
  'need': { phonetic: '/niːd/', definition: 'v./n. 需要' },
  'must': { phonetic: '/mʌst/', definition: 'v. 必须' },
  'i': { phonetic: '/aɪ/', definition: 'pron. 我' },
  'me': { phonetic: '/miː/', definition: 'pron. 我（宾格）' },
  'my': { phonetic: '/maɪ/', definition: 'pron. 我的' },
  'we': { phonetic: '/wiː/', definition: 'pron. 我们' },
  'us': { phonetic: '/ʌs/', definition: 'pron. 我们（宾格）' },
  'our': { phonetic: '/aʊər/', definition: 'pron. 我们的' },
  'you': { phonetic: '/juː/', definition: 'pron. 你；你们' },
  'your': { phonetic: '/jʊr/', definition: 'pron. 你的' },
  'he': { phonetic: '/hiː/', definition: 'pron. 他' },
  'him': { phonetic: '/hɪm/', definition: 'pron. 他（宾格）' },
  'his': { phonetic: '/hɪz/', definition: 'pron. 他的' },
  'she': { phonetic: '/ʃiː/', definition: 'pron. 她' },
  'her': { phonetic: '/hɜːr/', definition: 'pron. 她的；她（宾格）' },
  'it': { phonetic: '/ɪt/', definition: 'pron. 它' },
  'its': { phonetic: '/ɪts/', definition: 'pron. 它的' },
  'they': { phonetic: '/ðeɪ/', definition: 'pron. 他们' },
  'them': { phonetic: '/ðem/', definition: 'pron. 他们（宾格）' },
  'their': { phonetic: '/ðeər/', definition: 'pron. 他们的' },
  'this': { phonetic: '/ðɪs/', definition: 'pron. 这个' },
  'that': { phonetic: '/ðæt/', definition: 'pron. 那个' },
  'these': { phonetic: '/ðiːz/', definition: 'pron. 这些' },
  'those': { phonetic: '/ðoʊz/', definition: 'pron. 那些' },
  'what': { phonetic: '/wʌt/', definition: 'pron. 什么' },
  'which': { phonetic: '/wɪtʃ/', definition: 'pron. 哪个' },
  'who': { phonetic: '/huː/', definition: 'pron. 谁' },
  'whom': { phonetic: '/huːm/', definition: 'pron. 谁（宾格）' },
  'whose': { phonetic: '/huːz/', definition: 'pron. 谁的' },
  'where': { phonetic: '/weər/', definition: 'adv. 在哪里' },
  'when': { phonetic: '/wen/', definition: 'adv. 什么时候' },
  'why': { phonetic: '/waɪ/', definition: 'adv. 为什么' },
  'how': { phonetic: '/haʊ/', definition: 'adv. 怎样' },
  'and': { phonetic: '/ænd/', definition: 'conj. 和' },
  'but': { phonetic: '/bʌt/', definition: 'conj. 但是' },
  'or': { phonetic: '/ɔːr/', definition: 'conj. 或者' },
  'not': { phonetic: '/nɒt/', definition: 'adv. 不' },
  'so': { phonetic: '/soʊ/', definition: 'adv./conj. 所以' },
  'if': { phonetic: '/ɪf/', definition: 'conj. 如果' },
  'then': { phonetic: '/ðen/', definition: 'adv. 然后' },
  'than': { phonetic: '/ðæn/', definition: 'conj. 比' },
  'too': { phonetic: '/tuː/', definition: 'adv. 也；太' },
  'very': { phonetic: '/ˈveri/', definition: 'adv. 非常' },
  'just': { phonetic: '/dʒʌst/', definition: 'adv. 只是；刚刚' },
  'also': { phonetic: '/ˈɔːlsoʊ/', definition: 'adv. 也' },
  'only': { phonetic: '/ˈoʊnli/', definition: 'adv./adj. 只；仅有的' },
  'even': { phonetic: '/ˈiːvən/', definition: 'adv. 甚至' },
  'still': { phonetic: '/stɪl/', definition: 'adv. 仍然' },
  'already': { phonetic: '/ɔːlˈredi/', definition: 'adv. 已经' },
  'always': { phonetic: '/ˈɔːlweɪz/', definition: 'adv. 总是' },
  'never': { phonetic: '/ˈnevər/', definition: 'adv. 从不' },
  'often': { phonetic: '/ˈɔːfən/', definition: 'adv. 经常' },
  'usually': { phonetic: '/ˈjuːʒuəli/', definition: 'adv. 通常' },
  'sometimes': { phonetic: '/ˈsʌmtaɪmz/', definition: 'adv. 有时' },
  'here': { phonetic: '/hɪər/', definition: 'adv. 这里' },
  'there': { phonetic: '/ðeər/', definition: 'adv. 那里' },
  'now': { phonetic: '/naʊ/', definition: 'adv. 现在' },
  'today': { phonetic: '/təˈdeɪ/', definition: 'adv./n. 今天' },
  'tomorrow': { phonetic: '/təˈmɒroʊ/', definition: 'adv./n. 明天' },
  'yesterday': { phonetic: '/ˈjestərdeɪ/', definition: 'adv./n. 昨天' },
  'up': { phonetic: '/ʌp/', definition: 'adv. 向上' },
  'down': { phonetic: '/daʊn/', definition: 'adv. 向下' },
  'out': { phonetic: '/aʊt/', definition: 'adv. 出去' },
  'in': { phonetic: '/ɪn/', definition: 'prep. 在…里' },
  'on': { phonetic: '/ɒn/', definition: 'prep. 在…上' },
  'at': { phonetic: '/æt/', definition: 'prep. 在' },
  'to': { phonetic: '/tuː/', definition: 'prep. 到；向' },
  'for': { phonetic: '/fɔːr/', definition: 'prep. 为了' },
  'with': { phonetic: '/wɪð/', definition: 'prep. 和；用' },
  'by': { phonetic: '/baɪ/', definition: 'prep. 通过；被' },
  'from': { phonetic: '/frʌm/', definition: 'prep. 从' },
  'of': { phonetic: '/ʌv/', definition: 'prep. 的' },
  'about': { phonetic: '/əˈbaʊt/', definition: 'prep./adv. 关于' },
  'into': { phonetic: '/ˈɪntuː/', definition: 'prep. 进入' },
  'over': { phonetic: '/ˈoʊvər/', definition: 'prep. 越过；在…上方' },
  'after': { phonetic: '/ˈæftər/', definition: 'prep./conj. 在…之后' },
  'before': { phonetic: '/bɪˈfɔːr/', definition: 'prep./conj. 在…之前' },
  'between': { phonetic: '/bɪˈtwiːn/', definition: 'prep. 在…之间' },
  'through': { phonetic: '/θruː/', definition: 'prep. 穿过' },
  'during': { phonetic: '/ˈdʊrɪŋ/', definition: 'prep. 在…期间' },
  'without': { phonetic: '/wɪˈðaʊt/', definition: 'prep. 没有' },
  'again': { phonetic: '/əˈɡen/', definition: 'adv. 再次' },
  'back': { phonetic: '/bæk/', definition: 'adv./n. 回来；后面' },
  'well': { phonetic: '/wel/', definition: 'adv. 好' },
  'right': { phonetic: '/raɪt/', definition: 'adj./n. 正确的；权利' },
  'old': { phonetic: '/oʊld/', definition: 'adj. 老的；旧的' },
  'new': { phonetic: '/njuː/', definition: 'adj. 新的' },
  'good': { phonetic: '/ɡʊd/', definition: 'adj. 好的' },
  'bad': { phonetic: '/bæd/', definition: 'adj. 坏的' },
  'big': { phonetic: '/bɪɡ/', definition: 'adj. 大的' },
  'small': { phonetic: '/smɔːl/', definition: 'adj. 小的' },
  'long': { phonetic: '/lɒŋ/', definition: 'adj. 长的' },
  'high': { phonetic: '/haɪ/', definition: 'adj. 高的' },
  'great': { phonetic: '/ɡreɪt/', definition: 'adj. 伟大的' },
  'own': { phonetic: '/oʊn/', definition: 'adj./v. 自己的；拥有' },
  'other': { phonetic: '/ˈʌðər/', definition: 'adj. 其他的' },
  'same': { phonetic: '/seɪm/', definition: 'adj. 相同的' },
  'another': { phonetic: '/əˈnʌðər/', definition: 'adj. 另一个' },
  'each': { phonetic: '/iːtʃ/', definition: 'adj. 每个' },
  'all': { phonetic: '/ɔːl/', definition: 'adj. 所有的' },
  'every': { phonetic: '/ˈevri/', definition: 'adj. 每一个' },
  'any': { phonetic: '/ˈeni/', definition: 'adj. 任何' },
  'few': { phonetic: '/fjuː/', definition: 'adj. 少数的' },
  'more': { phonetic: '/mɔːr/', definition: 'adj. 更多的' },
  'most': { phonetic: '/moʊst/', definition: 'adj. 最多的' },
  'much': { phonetic: '/mʌtʃ/', definition: 'adj. 许多' },
  'many': { phonetic: '/ˈmeni/', definition: 'adj. 许多' },
  'some': { phonetic: '/sʌm/', definition: 'adj. 一些' },
  'no': { phonetic: '/noʊ/', definition: 'adj. 没有' },
  'not': { phonetic: '/nɒt/', definition: 'adv. 不' },
  'yes': { phonetic: '/jes/', definition: 'adv. 是' },
  'please': { phonetic: '/pliːz/', definition: 'adv. 请' },
  'thank': { phonetic: '/θæŋk/', definition: 'v. 感谢' },
  'sorry': { phonetic: '/ˈsɒri/', definition: 'adj. 对不起' },
  'hello': { phonetic: '/həˈloʊ/', definition: 'int. 你好' },
  'goodbye': { phonetic: '/ɡʊdˈbaɪ/', definition: 'int. 再见' },
  'because': { phonetic: '/bɪˈkʌz/', definition: 'conj. 因为' },
  'although': { phonetic: '/ɔːlˈðoʊ/', definition: 'conj. 虽然' },
  'while': { phonetic: '/waɪl/', definition: 'conj. 当…时' },
  'until': { phonetic: '/ənˈtɪl/', definition: 'conj. 直到' },
  'since': { phonetic: '/sɪns/', definition: 'conj. 自从' },
  'whether': { phonetic: '/ˈweðər/', definition: 'conj. 是否' },
  'however': { phonetic: '/haʊˈevər/', definition: 'adv. 然而' },
  'therefore': { phonetic: '/ˈðeərfɔːr/', definition: 'adv. 因此' },
  'perhaps': { phonetic: '/pərˈhæps/', definition: 'adv. 也许' },
  'together': { phonetic: '/təˈɡeðər/', definition: 'adv. 一起' },
  'enough': { phonetic: '/ɪˈnʌf/', definition: 'adj./adv. 足够的' },
  'really': { phonetic: '/ˈriːəli/', definition: 'adv. 真正地' },
  'almost': { phonetic: '/ˈɔːlmoʊst/', definition: 'adv. 几乎' },
  'around': { phonetic: '/əˈraʊnd/', definition: 'prep./adv. 围绕' },
  'away': { phonetic: '/əˈweɪ/', definition: 'adv. 离开' },
  'quite': { phonetic: '/kwaɪt/', definition: 'adv. 相当' },
  'again': { phonetic: '/əˈɡen/', definition: 'adv. 再次' }
}

const commonWords = new Set([
  'a', 'an', 'the', 'is', 'are', 'was', 'were', 'be', 'been', 'being',
  'have', 'has', 'had', 'do', 'does', 'did', 'will', 'would', 'could',
  'should', 'may', 'might', 'shall', 'can', 'need', 'dare', 'ought',
  'i', 'me', 'my', 'mine', 'we', 'us', 'our', 'ours',
  'you', 'your', 'yours', 'he', 'him', 'his', 'she', 'her', 'hers',
  'it', 'its', 'they', 'them', 'their', 'theirs',
  'this', 'that', 'these', 'those', 'who', 'whom', 'whose', 'which', 'what',
  'and', 'but', 'or', 'nor', 'not', 'so', 'yet', 'both', 'either', 'neither',
  'in', 'on', 'at', 'to', 'for', 'with', 'by', 'from', 'of', 'about',
  'into', 'through', 'during', 'before', 'after', 'above', 'below',
  'between', 'out', 'off', 'over', 'under', 'again', 'further',
  'then', 'once', 'here', 'there', 'when', 'where', 'why', 'how',
  'all', 'each', 'every', 'both', 'few', 'more', 'most', 'other',
  'some', 'such', 'no', 'nor', 'too', 'very', 'just', 'because',
  'as', 'until', 'while', 'although', 'though', 'if', 'unless',
  'since', 'so that', 'rather', 'than', 'whether'
])

export function splitParagraphs(text) {
  const normalized = text.replace(/\r\n/g, '\n').replace(/\r/g, '\n')
  return normalized
    .split('\n')
    .map(line => line.trim())
    .filter(Boolean)
}

export function parseArticle(text) {
  const sentences = text.match(/[^.!?]+[.!?]+/g) || [text]
  const words = []
  const wordRegex = /[a-zA-Z]+(?:'[a-zA-Z]+)?/g

  let match
  while ((match = wordRegex.exec(text)) !== null) {
    const word = match[0].toLowerCase()
    if (word.length > 1 && !commonWords.has(word)) {
      words.push(word)
    }
  }

  return {
    sentences: sentences.map(s => s.trim()).filter(Boolean),
    paragraphs: splitParagraphs(text),
    words: [...new Set(words)]
  }
}

export function getWordContext(text, word, maxWords = 0, occurrence = 0) {
  const wordRegex = new RegExp(`\\b${word}\\b`, 'gi')
  let match
  let count = 0
  let wordIndex = -1
  while ((match = wordRegex.exec(text)) !== null) {
    if (count === occurrence) {
      wordIndex = match.index
      break
    }
    count++
  }
  if (wordIndex < 0) return ''
  const wordLength = word.length

  const sentenceEnders = /[.!?]/
  let start = wordIndex
  while (start > 0 && !sentenceEnders.test(text[start - 1])) {
    start--
  }

  let end = wordIndex + wordLength
  while (end < text.length && !sentenceEnders.test(text[end])) {
    end++
  }
  if (end < text.length) end++

  let context = text.substring(start, end).trim()

  if (maxWords > 0) {
    const words = context.split(/\s+/)
    if (words.length > maxWords) {
      const targetIdx = words.findIndex(w => new RegExp(`^${word}`, 'i').test(w))
      const target = targetIdx >= 0 ? targetIdx : 0
      const half = Math.floor(maxWords / 2)
      const from = Math.max(0, target - half)
      context = words.slice(from, from + maxWords).join(' ')
    }
  }

  return context
}

// 句子边界：句末标点(.!?)及其后的引号/括号（后跟空白或文末），或引语前的冒号（后跟空白）
// 供“单词在文中翻译”与“划词翻译语境提取”共用，避免两处规则漂移
export const SENTENCE_BOUNDARY_REGEX = /[.!?]+["')\]]*(?=\s|$)|:(?=\s)/g

// 提取目标词所在句子及前后语境，用于“在文中”翻译：
// sentence 为目标词所在的单句（限定翻译输出范围，保证显示简短），
// context 额外带上前后各 extraSentences 句（传给 AI 帮助其贴合语境理解）
export function getWordSentenceWithContext(text, word, occurrence = 0, extraSentences = 1) {
  const wordRegex = new RegExp(`\\b${word}\\b`, 'gi')
  let match
  let count = 0
  let wordIndex = -1
  while ((match = wordRegex.exec(text)) !== null) {
    if (count === occurrence) {
      wordIndex = match.index
      break
    }
    count++
  }
  if (wordIndex < 0) return { sentence: '', context: '' }

  // 共享的带 g 标志正则有 lastIndex 状态，使用前重置
  SENTENCE_BOUNDARY_REGEX.lastIndex = 0
  const sentences = []
  let start = 0
  let b
  while ((b = SENTENCE_BOUNDARY_REGEX.exec(text)) !== null) {
    const end = b.index + b[0].length
    const piece = text.slice(start, end).trim()
    if (piece) sentences.push({ start, end, text: piece })
    start = end
  }
  if (start < text.length) {
    const piece = text.slice(start).trim()
    if (piece) sentences.push({ start, end: text.length, text: piece })
  }

  const targetIdx = sentences.findIndex(s => wordIndex >= s.start && wordIndex < s.end)
  if (targetIdx < 0) return { sentence: '', context: '' }

  const sentence = sentences[targetIdx].text
  const from = Math.max(0, targetIdx - extraSentences)
  const to = Math.min(sentences.length - 1, targetIdx + extraSentences)
  const context = sentences.slice(from, to + 1).map(s => s.text).join(' ')
  return { sentence, context }
}

// 划词翻译：选中文本的规范化（小写 + 空白折叠 + trim），
// 兼容跨段落选择时 selection.toString() 与原文空白形态不一致的差异
export function normalizeSelectionText(text) {
  return String(text || '').toLowerCase().replace(/\s+/g, ' ').trim()
}

// 划词翻译：定位选中文本在原文中的位置，返回其所在句前后各 extraSentences 句的语境。
// 定位失败（如选区与原文不一致）返回空串，翻译降级为无语境，功能不中断。
export function getSelectionContext(fullText, selection, extraSentences = 1) {
  const source = String(fullText || '')
  const needle = normalizeSelectionText(selection)
  if (!source || !needle) return ''

  // 原文按同策略归一化（空白折叠为单空格），并保留「折叠文本索引 → 原文索引」映射，
  // 使跨段落选择（选区字符串与原文空白形态不一致）也能定位
  let folded = ''
  const indexMap = []
  let i = 0
  while (i < source.length) {
    if (/\s/.test(source[i])) {
      folded += ' '
      indexMap.push(i)
      while (i < source.length && /\s/.test(source[i])) i++
    } else {
      folded += source[i].toLowerCase()
      indexMap.push(i)
      i++
    }
  }
  const pos = folded.indexOf(needle)
  if (pos < 0 || pos + needle.length > indexMap.length) return ''

  const startIdx = indexMap[pos]
  const endIdx = indexMap[pos + needle.length - 1] + 1

  SENTENCE_BOUNDARY_REGEX.lastIndex = 0
  const sentences = []
  let s = 0
  let b
  while ((b = SENTENCE_BOUNDARY_REGEX.exec(source)) !== null) {
    const end = b.index + b[0].length
    const piece = source.slice(s, end).trim()
    if (piece) sentences.push({ start: s, end, text: piece })
    s = end
  }
  if (s < source.length) {
    const piece = source.slice(s).trim()
    if (piece) sentences.push({ start: s, end: source.length, text: piece })
  }

  // 选中片段可与多句相交：取首个与末个相交句作锚点，语境前后各扩 extraSentences 句，保证完整覆盖选区
  let targetIdx = -1
  let lastIdx = -1
  for (let i = 0; i < sentences.length; i++) {
    if (startIdx < sentences[i].end && endIdx > sentences[i].start) {
      if (targetIdx < 0) targetIdx = i
      lastIdx = i
    }
  }
  if (targetIdx < 0) return ''

  const from = Math.max(0, targetIdx - extraSentences)
  const to = Math.min(sentences.length - 1, lastIdx + extraSentences)
  return sentences.slice(from, to + 1).map(x => x.text).join(' ')
}

export function getAllOccKeys(text) {
  const keys = []
  const counts = {}
  const wordRegex = /[a-zA-Z]+(?:'[a-zA-Z]+)?/g
  let match
  while ((match = wordRegex.exec(text)) !== null) {
    const word = match[0].toLowerCase()
    if (word.length > 1) {
      const count = counts[word] || 0
      counts[word] = count + 1
      keys.push(`${word}:${count}`)
    }
  }
  return keys
}

export function lemmatize(word) {
  const rules = [
    { suffix: 'ies', replace: 'y' },
    { suffix: 'ves', replace: 'f' },
    { suffix: 'ses', replace: 's' },
    { suffix: 'xes', replace: 'x' },
    { suffix: 'zes', replace: 'z' },
    { suffix: 'ches', replace: 'ch' },
    { suffix: 'shes', replace: 'sh' },
    { suffix: 'es', replace: '' },
    { suffix: 's', replace: '' },
    { suffix: 'ied', replace: 'y' },
    { suffix: 'ed', replace: '' },
    { suffix: 'ing', replace: '' },
    { suffix: 'ly', replace: '' },
    { suffix: 'er', replace: '' },
    { suffix: 'est', replace: '' }
  ]

  const lower = word.toLowerCase()

  for (const rule of rules) {
    if (lower.endsWith(rule.suffix)) {
      const base = lower.slice(0, -rule.suffix.length) + rule.replace
      if (base.length > 2) {
        return base
      }
    }
  }

  return lower
}
